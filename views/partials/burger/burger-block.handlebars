<li>
    {{burger_name}}

    <button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
        {{#if devoured}}NOT TODAY!{{else}}NOM! NOM!{{/if}}
    </button>
    {{!-- <button class="delete-burger" data-id="{{id}}">DELETE!!</button> --}}
</li>